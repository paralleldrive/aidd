# README Skill Link Fix Epic

## Summary

The README.md skills table contained a broken link after the `aidd-functional-requirements`
skill was renamed to `aidd-requirements`. The link pointed to a path that no longer exists.

## Requirements

- Given README.md skills table, should link `/aidd-requirements` to `ai/skills/aidd-requirements/README.md`
- Given README.md skills table, should not reference the removed `ai/skills/aidd-functional-requirements/README.md` path
